// Registry entry for the "blog / cro1-blog" builder template (Elementor 55021). Loaded lazily by
// ../registry.ts so only the rendered template's stylesheet ships with the page.
import "@/app/shared/pages/493b4e2c7ef5.css";
export { default } from "@/components/documents/Page55021";
