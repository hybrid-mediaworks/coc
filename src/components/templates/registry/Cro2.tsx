// Registry entry for the "cro2" builder template (Elementor 54508). Loaded lazily by
// ../registry.ts so only the rendered template's stylesheet ships with the page.
import "@/app/shared/pages/8efd3f1fd4cd.css";
export { default } from "@/components/documents/Page54508";
