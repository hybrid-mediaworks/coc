// Registry entry for the "cro1" builder template (Elementor 54439). Loaded lazily by
// ../registry.ts so only the rendered template's stylesheet ships with the page.
import "@/app/shared/pages/227b915e142f.css";
export { default } from "@/components/documents/Page54439";
