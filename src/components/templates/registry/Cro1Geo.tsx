// Registry entry for the "cro1-geo" builder template (Elementor 55188). Loaded lazily by
// ../registry.ts so only the rendered template's stylesheet ships with the page.
import "@/app/shared/pages/be46224455b9.css";
export { default } from "@/components/documents/Page55188";
